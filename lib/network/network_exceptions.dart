abstract class ErrorHandler {
  onError({String? message, int? code, String? error});
}

// class AuthException extends ErrorHandler {
//   AuthException({message, code}) : super(message: message, code: code);
// }
